#include <iostream>

using namespace std;
int main()
{
  //  int a,b;
  // cin>>a>>b;

  // if(a%2>0 && b%2>0)
  // cout<<a*b;

  // else if (a%2==0 && b%2==0)
  //  cout<<a/b;

  // else if (a%2>0 && b%2==0)
  //  cout<<a+b;

  // else if (a%2==0 && b%2>0)
  // cout<<a-b;

  // cout << "\n"
  //  << "\n***********************\n";

  // int a, b, c;
  // cin >> a >> b >> c;

  // if (a > b && a > c)
  //  {
  //  if (b > c)
  //  {
  //    cout << c << " " << b << " " << a;
  //}
  //  if (c > b)
  //{
  // cout << b << " " << c << " " << a;
  //}
  //}

  // if (b > c && b > a)
  //{
  //   if (a > c)
  //   {
  //    cout << c << " " << a << " " << b;
  // }
  // if (c > a)
  // {
  // cout << a << " " << c << " " << b;
  // }
  //}

  // if (c > b && c > a)
  //{
  // if (b > a)
  //{
  //   cout << a << " " << b << " " << c;
  // }
  // if (a > b)
  //{
  // cout << b << " " << a << " " << c;
  // }
  //}
  // cout<<"\n"<<"*********************************\n";
  // int  start ,end;
  // cin>>start>>end;
  // while(start<=end){

  //  cout<<start<<"\n";

  //  start++;
  //}

  // cout << "\n"
  //   << "****************\n";
  // int num;
  // char ch;
  // cin >> num >> ch;
  // while (num > 0)
  // {
  //  cout << ch;
  //  num -= 1;
  // }

  // cout << "\n"
  //  << "************************\n";
  // int a,b,c;
  // cin>>a>>b>>c;
  // int res=0;

  // if( a>res &&a<100){
  //   res=a;
  // }
  // else if( b>res && b<100){
  // res=b;}

  // else if( c>res && c<100){
  // res=c ;}
  // cout<<res<<"\n";

  // if (a>=100 && b>=100 && c>=100){
  // cout<<"-1"<<"\n";}

  // cout<<"\n"<<"********************\n";
  // int x,a,b,c,d,e;

  // cin>>x>>a>>b>>c>>d>>e;

  // int cnt=0;
  // cnt+=(a<=x);
  // cnt+=(b<=x);
  // cnt+=(c<=x);
  // cnt+=(d<=x);
  // cnt+=(e<=x);

  // cout<<cnt<<" "<<5-cnt<<"\n";

  cout << "\n"
       << "********************\n"
        
        for(int x =51 ; x<300 && x>50 ; x++);
        for(int y =71

      return 0;
}